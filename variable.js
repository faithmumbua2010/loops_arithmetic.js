// it is a labelled box where you store information and you can use later

let name="faith"; // we use 'let' when value might change
let age=21;
let is_student= true;
const country= "Kenya"; // we use 'const' when value stays constant

console.log(name); // faith
// console.log() is the print() in JS
console.log(country);
// we avoid using var because it is a bit old and it lives it open


